destroyed entryway
- if activity.parameters[:entryway]
  - activity.parameters[:entryway].each do |entryway_id|
    - entryway = DataEntryway.find(entryway_id)
    - if entryway
      %span= entryway.did_e164
on station
- if activity.trackable  
  = link_to_settings_station(activity.trackable.id)
- else
  which has since been removed