added prompt
- if activity.parameters[:prompt_id]
  - prompt = DataGatewayPrompt.find(activity.parameters[:prompt_id]) rescue nil
  - if prompt
    %span= prompt.title
on station
- if activity.trackable
  = link_to_settings_station(activity.trackable.id)
- else
  which has since been removed