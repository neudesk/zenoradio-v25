untagged RCA 
- if activity.parameters[:old_rca]
  %span= DataGroupRca.find(activity.parameters[:old_rca]).title if DataGroupRca.find(activity.parameters[:old_rca])
on a station
- if activity.trackable
  = link_to_settings_station(activity.trackable.id) 
- else
  which has since been removed