tagged RCA 
- if activity.parameters[:new_rca]
  %span= DataGroupRca.find(activity.parameters[:new_rca]).title if DataGroupRca.find(activity.parameters[:new_rca])
  on a station
- if activity.trackable
  = link_to_settings_station(activity.trackable.id)  
- else
  which has since been removed